import { DescriptionLimitPipe } from './description-limit-pipe';

describe('DescriptionLimitPipe', () => {
  it('should create an instance', () => {
    expect(new DescriptionLimitPipe()).toBeTruthy();
  });
});
